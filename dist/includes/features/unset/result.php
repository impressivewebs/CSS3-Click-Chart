<div class="result-unset">
  <p>This paragraph's font family is "unset", so the value computes to its parent's font.</p>

  <p><b>This sentence is inside a <code>&lt;b&gt;</code> element. Its font weight is "unset" and therefore just looks normal instead of bold.</b></p>
</div>